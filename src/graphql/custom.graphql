query userCustoms {
  userCustoms {
    items {
      id,
      name,
      zh_name,
      plural_name
    },
    cursor {
      total
    }
  }
}

query userCustom ($id: Int!) {
  userCustom (id: $id) {
    id,
    name,
    zh_name,
    plural_name
  }
}
